# 0.4.3：原生视觉基础

状态：PRD 已建立，尚未实施/验收/发布。依赖：0.4.2；最终属于 [0.5 路线](../prd-android-05.md)。

## 更新计划

1. 抽出集中式原生主题，完整定义浅深语义色、字号层级和圆角；继续使用既有靛蓝/琥珀品牌。
2. 将主题接入 App，并让抽屉、欢迎页和工作台标题消费统一层级；消除明显的硬编码低对比文字。
3. 修复欢迎建议可能替换非空输入的入口；保留导航结构与所有 API 行为。
4. subagent 提交实现和新增测试；主 agent 审查 diff、运行回归、查看截图后决定是否提升候选版本。

## PRD

### US-431：长时间阅读时区分内容层级

作为手机用户，我需要在浅深主题下分清标题、正文、补充信息和主要动作，不被多余背景或过小的标签干扰。

- FR-431：新增独立 NativeTheme 主题模块；完整提供 primary/secondary/tertiary/error 的前景、背景、容器色及 surface 层级，避免默认 Material 色意外混入。
- FR-432：定义 body 16sp/24sp、secondary 14sp/20sp、label 至少 12sp、title 20sp/28sp 的基准层级；聊天自定义 12–24sp 偏好继续生效。实际必要例外需注明。
- FR-433：以 8/12/16/24dp 圆角语义控制层级；普通消息不额外加厚边框。深色不把所有面板染成高饱和蓝。
- FR-434：正文/次要文字对比度目标 4.5:1；至少对浅深主题的正文、辅助文本、主按钮与错误容器做自动测试。

### US-432：在欢迎页安全开始任务

作为用户，点击起始建议只在草稿为空且可编辑时回填，不替换已有内容，也不会发送或创建服务器任务。

- FR-435：有文字、引用、附件或发送不可用状态时不覆盖输入；禁用状态和实际行为一致。
- FR-436：底栏继续为 48dp、三图标、无可见标签，保持测试语义、键盘显隐和选中样式。

## 实施边界

允许：`NativeTheme.kt`、`NativeApp.kt` 主题接线、`MobileShell.kt`；独立新增主题/欢迎页测试。不修改 ClientModel、API、签名、版本、发布目录或生产服务。除非主 agent 明确重新分配，不修改现有共享 UI 测试文件。

## QA / 完成标准

- [ ] JVM 测试通过；新增主题配色/对比度或等价确定性测试。
- [ ] 原生 UI：浅深主题正确；欢迎建议只回填空草稿，非空/带附件不覆盖，不调用发送。
- [ ] 360dp 浅色、411dp 深色及 1.5x/2x 字体截图；主 agent 目视确认层级和无关键遮挡。
- [ ] 0.4.2 的底栏 48dp、无文字、语义、IME、抽屉折叠、草稿保持回归通过。
- [ ] Android lint/build 与 `npm test`；记录实际结果及既有警告。
- [ ] 不使用浏览器预览冒充原生验收；若涉及网页则另用 Playwright/dev-browser 验证。

## 验收记录

实施 subagent：Ampere；独立 UI QA：Hubble。主 agent 审查：待实现返回。

基线 `npm test`：沙箱内 16 个测试文件因受限环境失败；沙箱外完整重跑 315/315 通过。Android 结果、截图与验收结论待填写；所有未勾选项均未通过验收。
