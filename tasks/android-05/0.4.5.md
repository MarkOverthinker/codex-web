# 0.4.5：工作台与变更检索

状态：内部开发里程碑已实现并通过主 agent 候选门禁，合入 0.5.0-preview；未独立发布。依赖：0.4.3 主题；与 0.4.4 并行实现、分别验收；属于 [0.5 路线](../prd-android-05.md)。

## 更新计划

1. 工作台从无差别工具清单调整为当前任务上下文、任务工具和全局管理三个层次。
2. 为 Git Review 增加已加载文件的路径搜索和类型筛选，保留来源范围、分支与总览。
3. 完整区分无变更、无匹配、读取失败及旧数据刷新失败。
4. subagent 实现独立筛选逻辑、原生 UI 和单元测试，主 agent 检查 API 一致性及返回路径。

## PRD

### US-451：在当前任务下找工具

作为用户，我需要知道工具作用于哪个任务与目录，并能两次操作内进入项目文件或 Review。

- FR-451：顶部显示当前任务标题与可用的目录名/状态；无任务时提供返回对话的明确动作，不虚构数据或创建任务。
- FR-452：任务队列、项目文件、代码 Review、侧线程归为任务工具；目录管理和用量统计归为全局管理，入口附简短用途。
- FR-453：保持现有工具路由、功能和权限；不显示不存在的远端分支或同步状态，不新增直接 Git 写 API。

### US-452：快速定位已加载的变更

作为用户，我需要按路径或状态缩小变更文件列表，同时知道是否仍有实际变更。

- FR-454：Review 提供不区分大小写的路径子串搜索和全部/新增/修改/删除/其他筛选，使用现有已加载文件，不新增后端搜索。
- FR-455：未知状态、重命名、未跟踪文件按明确策略归类并测试；总览统计仍基于全部文件，筛选显示“匹配数 / 总数”。
- FR-456：筛选无结果显示“没有匹配文件”和清除操作，不显示“没有变更”；网络错误继续展示错误和重试。
- FR-457：进入单文件差异后返回保留搜索/筛选及合理阅读位置；更换任务、scope/base 不串入旧任务状态。筛选条件改变时回到结果开头。
- FR-458：保留长文件名信息、未知行数、删除/重命名和差异横向滚动；不把缩短列表称为服务器全库搜索。

## 实施边界

允许：`MobileShell.kt` 的 WorkspaceHome、`ToolsScreen.kt` 的 Review、独立筛选文件和测试；不得改模型提交协议、队列动作或 Web。0.4.3 已验收后执行；与 0.4.4 的 NativeApp 写入范围不重叠，可并行实施。

## QA / 完成标准

- [x] 路径大小写、空白、Unicode、状态组合、未跟踪/重命名/未知状态的单元测试通过。
- [x] 原生 UI：搜索、清空、状态筛选、0 匹配、无变更、错误重试各自断言；不能因筛选增加 API 调用。
- [x] 打开 diff 后返回保留筛选；任务或范围切换无旧状态污染。
- [x] 短列表/长列表、长文件名、360dp/深色/2x 字体截图；返回对话保留草稿。
- [x] 空任务时不会新增服务器任务；原有侧线程/目录/统计入口仍可达。
- [x] Android unit/lint/build、原生 UI、`npm test`；网页变动另用 Playwright/dev-browser 验证。

## 验收记录

实施 subagent：Boyle；独立 UI QA：Hubble；主 agent 负责代码审查、模拟器执行与最终签收。

Review 筛选独立单元测试 14/14，覆盖五种筛选、空白/大小写/Unicode、未知状态、原始次序与 JSON 不变。仅对已加载文件本地处理，保留整体统计，范围/基准切换清空当前筛选；diff 返回保留条件。

主 agent 核对任务上下文、无任务返回、无匹配/无变更/错误区分及原 API 路由；修复重复工作台标题、搜索时滚动目标和真实 diff 横向滚动节点标记。IME 用例明确等待键盘就绪，连续输入保留焦点和请求计数断言。

首轮一次新草稿输入框显示为空，旧日志不足以证明模型或持久化内容丢失；后续增加七个检查点和三次快速往返，未重写草稿或改 ClientModel，重复回归通过。最终数量、布局矩阵及签收见 [0.5 集成验收](0.5.0.md)。


最终签收（2026-09-12）：Android JVM 130/130、完整原生 36/36、目标跟随/IME 3/3、三组布局各 3/3、发送按钮完整可见性各 1/1、`npm test` 315/315；lintPreview 0 错误/18 既有警告。浅深截图经主 agent 检查；业务 API 为模拟数据，真机与真实账户不在此次签收范围。
