# 0.5.1：一级页面横向滑动切换

日期：2026-09-13。基线：已发布的 `0.5.0-preview` / versionCode 12。状态：源码实现与独立 QA 已通过；2026-09-13 用户明确要求发布后，提升至 versionCode 13 构建 `0.5.1-preview` 签名候选并发布至固定下载页，发布证据见 [0.5.1-preview](../../releases/android/0.5.1-preview.md)。

## 更新计划

1. 协调 agent 固定手势范围、切换顺序、冲突边界与验收标准。
2. 实施 subagent 只修改一级页面容器及对应确定性测试，实现「对话 ↔ 工作台 ↔ 我的」横向分页和底栏联动。
3. 独立 QA subagent 不改生产代码，运行单元、构建和原生 UI 手势回归并记录证据；失败项退回实施方。
4. 协调 agent 审查差异、实际测试和文档同步；不自动提升版本号、构建签名候选或公开发布。

## PRD

### 用户故事

作为 App 用户，我希望在三个一级页面中直接左右滑动切换，并看到连续、跟手的页面过渡，同时仍可使用底部导航按钮。

### 功能要求

- FR-511：一级页面顺序固定为「对话、工作台、我的」；左滑进入右侧相邻页，右滑进入左侧相邻页，到达两端后不循环。
- FR-512：拖动过程呈现连续的横向位移，松手后平滑归位或落到目标页；底栏点击也使用同一分页状态和动画。
- FR-513：分页落定后 `HomeTab` 与底栏选中状态一致；切换期间不得发送消息、清空草稿、重置对话阅读位置或额外请求业务接口。
- FR-514：手势只在一级页面生效。工具详情页、模态弹窗、任务抽屉和键盘输入场景维持现有交互；系统返回逻辑与底栏的可访问标签继续有效。
- FR-515：在对话、工作台和我的三个页面验证双向相邻切换、端点不越界、快速滑动和短距离拖动回弹。

### 非目标与限制

- 不新增页面、不改变服务端协议、持久化、租户 UID 或发布配置。
- 不将本轮源码改动称为 0.5.1 已发布版本；真机厂商手势导航兼容性需要后续真实设备复核。
- 页面内部可横向滚动的内容应优先保留自身手势；本轮验收至少覆盖主内容空白区域的分页滑动。

## 可验证 QA 清单

- [x] 文档在生产代码变更前提交，范围与非目标明确。
- [x] 从对话左滑到工作台、再左滑到我的；反向右滑逐页返回对话，标题和底栏选中项同步。
- [x] 对话页右滑、我的页左滑均不越界；短拖动不误切页，完整滑动只有一次页面变化。
- [x] 底栏点击仍可切换三页，并呈现平滑过渡。
- [x] 页面往返后草稿和对话阅读位置保持，测试网关发送计数不增加。
- [x] 工具详情页不启用一级分页；任务抽屉、队列/编辑/确认弹窗及键盘场景无新增冲突。
- [x] Android JVM 测试、相关 Compose instrumentation、`lintDebug` 与 `assembleDebug` 通过并记录实际结果。
- [x] 仓库要求的 `npm test` 通过；仅改 Android 时不运行服务 reload。
- [x] 协调 agent 审查代码、测试证据和相关 Android 文档同步状态；记录尚未真机验证的限制。

## 验收记录

实施 subagent 修改 `NativeApp.kt`、`MobileShell.kt` 与 `NativeUiTest.kt`；独立 QA subagent 不改生产或测试源码，只复核差异并运行测试。协调 agent 审查并退回了陈旧状态、抽屉手势仲裁和快速 fling 证据三项，修复或补测后再验收。

| 门禁 | 实际结果 |
| --- | --- |
| 分页行为 | Compose `HorizontalPager` 按「对话 → 工作台 → 我的」非循环分页；底栏走同一状态动画；落页后模型、标题与选中项一致 |
| 手势边界 | 中央区域左右滑分页；对话页左侧 32dp 边缘右滑保留任务抽屉；工具详情、模态、IME、抽屉打开期间不启用分页 |
| 目标原生测试 | 新增 3/3；相关既有回归 8/8；协调 agent 再跑新增 3/3 |
| 完整原生测试 | API 36 模拟器 39/39，0 跳过、0 失败 |
| 快速滑动补充 | 同为 22% 距离：80ms 双向 fling 切页，600ms 拖动回弹；实施复跑 2 次、独立 QA 1/1，全部通过 |
| Android JVM / 工程 | 强制重跑 130/130；`lintDebug` 0 错误、18 个既有警告；Debug 与 AndroidTest APK 构建成功 |
| Web / 服务回归 | 会话默认临时目录存在跨挂载点路径别名时 326/328，两个路径断言可复现失败；改用无别名的系统 `/tmp` 后完整 `npm test` 328/328 |

实现提交：`historical-reference-removed`；快速 fling 测试提交：`historical-reference-removed`。本轮只改 Android，因此未运行 `npm run reload`。

## 发布记录（2026-09-13）

用户在里程碑验收后明确要求发布。协调 agent 提升 versionCode 12 → 13、versionName → `0.5.1` 后重建签名候选，本轮迭代约束（不提升版本、不构建候选、不发布）就此解除；发布为单独授权的动作，不改变上述 QA 证据的执行时点。

| 门禁 | 实际结果 |
| --- | --- |
| 版本提升后重建 | preview/release 变体 JVM 单测重新执行 260/260（debug 变体沿用同源码结果，合计 390 项 0 失败）；`lintPreview` 0 错误、18 项既有警告；持久签名 Preview 构建通过 |
| 签名与身份 | `verify-android-preview.sh` 复核通过：原可信证书、包名 `app.codexweb.mobile.preview`、SDK 26/36、ARM64、非调试/非测试、ZIP/16KB 对齐 |
| 覆盖升级 | 模拟器 0.5.0（code 12）→ 0.5.1 `adb install -r` 成功，`firstInstallTime` 保持，冷启动正常（截图 `tmp/android-release-0.5.1-launch.png`） |
| 公开发布 | `publish-android.mjs` 写入不可变版本文件；本地与公网页面/清单/校验文件 200，范围请求 206；公网完整重下载哈希 `7c949c54…` 与候选一致并再次通过校验脚本；10 条公开记录，9 个旧版本 APK 哈希未变 |
| 页面布局 | 本地等价页 390px/1280px 截图无水平溢出，版本与下载目标正确；无交互浏览器后端，公网浏览器导航未执行 |

同会话网页端改动（favicon）已另行 `npm test` 328/328 并 `npm run reload` 重载服务；Android 发布本身不需要重载 Web 服务。

剩余限制：分页手势只在 API 36 模拟器验收，未覆盖厂商真机与系统手势导航；没有保留拖动中间帧录像，也未单独覆盖页面内部嵌套横向滚动控件；Flyme 真机与真实账户业务验收仍未完成。当前公开下载为 `0.5.1-preview` / versionCode 13。
