# 0.5.0-preview 候选

候选日期：2026-09-12。versionCode：12。包名：`app.codexweb.mobile.preview`。

**状态：本地签名候选已完成本轮验收，迭代已停止；未公开发布。固定下载页仍为 0.4.2；公开发布需单独确认，不能把这份记录或 APK 附件理解为已完成发布。**

## 更新

- 统一原生浅深主题、语义色、字号和圆角；保留 48dp 三图标导航与系统安全区。
- 欢迎建议不覆盖非空草稿、引用、附件或关联来源，支持小屏/大字体滚动，不自动发送。
- 输入区准确区分发送、排队意图、转写、缓存和待核对状态，保留服务器确认与重复提交保护。
- 优化作者/操作层级、历史阅读与“回到最新”；区分用户滚动意图和布局变化，处理高于视口的末条回复。
- 工作台按当前任务与全局管理分组；无任务可直接返回原草稿。
- Review 支持已加载文件的路径/类型筛选、匹配计数及独立空态；进 diff 返回保留，切换范围或基准清空；不增加后端搜索或 Git 写 API。

0.4.3、0.4.4、0.4.5 是本轮内部开发里程碑，均有独立更新计划、PRD 与 QA；未伪造这些中间版本的公开发布记录。实施由 Ampere、Poincare、Boyle 承担，Hubble 编写独立 UI QA，主 agent 负责代码审查、退回修复、真正在模拟器执行和截图签收。详情见 [0.5 验收账本](../../tasks/prd-android-05.md)。

## 候选验证

- Android JVM：130/130；`lintPreview` 无错误、18 项既有警告；Debug/AndroidTest/持久签名 Preview 构建通过。
- 最终原生完整回归已通过 36/36；包含 UI、真实 Activity 重建和主题测试，业务 API 使用模拟数据。
- 最终自动跟随、历史阅读/超高末条到底和真实 IME 收展目标回归 3/3；三次快速草稿往返也在完整套件中通过。
- 360dp 搜索在显式等待真实 IME 就绪后连续三次通过；保留输入前后及隐藏键盘前后检查，不删除内容或请求数量断言。
- 360dp 浅色/1x、411dp 深色/1.5x、411dp 深色/2x 三组各 3/3；额外发送按钮完整可见性各 1/1。主 agent 检查浅深截图，保持 48dp 触控区域。
- 0.4.2 → 0.5.0 通过 `adb install -r` 覆盖升级，UID 和首次安装时间保持，冷启动成功；未卸载、未使用测试或降级绕过参数。
- 持久签名、包名、SDK、ARM64、非调试/非测试标记、ZIP/16KB 对齐通过。
- 服务器/Web 未改动；`npm test` 315/315。不需要重载 Web 服务。

## 审查历史与限制

主 agent 退回并修复了引用前景误配、初次尾部未稳定与后续布局重测无补偿、刷新反馈导致跟随意图丢失、搜索框滚到摘要后方及重复标题。测试侧修正了缺失的 diff 标签、旧标语断言、Compose API 返回类型和 IME 初始化同步；所有安全断言保留。

首轮一次新草稿输入框显示为空，日志当时未证明模型或持久化内容丢失；未通过修改模型或重写草稿来掩盖。后续增加七个检查点及不重写输入的快速往返，三轮重复与完整回归通过。仍需真实设备与真实 IME 继续验收。

尚未完成魅族/Flyme 真机和所有真实账号、计费、录音、文件授权及侧线程业务验收。模拟器不能替代真机，模拟 API 不能替代生产账号。公开下载页、HTTPS APK 重新下载及公开浏览器验收**尚未执行**，因为公开发布未获确认。

## 文件

文件名：`codex-native-android-0.5.0-preview.apk`；13,200,574 字节。

SHA-256：`271f073c3f29b65f117a404079ac962db37aa8f31bb8d5066d6c2549fccdcf58`。

本地候选位于仓库忽略目录 `tmp/android-05-qa/`，附同名 `.sha256`。已有同签名 preview 包应使用覆盖安装，不要卸载；不要用 `-t` 或降级参数绕过安装校验。正式公开发布继续使用 [维护流程](../../docs/ANDROID_RELEASES.md)。


本轮仅收尾此候选，不开始下一版本。最终命令、日志和截图来源见 [集成验收记录](../../tasks/android-05/0.5.0.md)；公开下载历史仍为原 8 个版本，最新 0.4.2。
