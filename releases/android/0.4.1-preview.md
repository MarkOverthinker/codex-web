# 0.4.1-preview

发布日期：2026-09-12。版本代码：10。包名：`app.codexweb.mobile.preview`。

## 更新

- 点击项目或状态分类标题可完整折叠，再次点击重新展开。
- 完整折叠与“仅显示前 4 个任务”分开处理，重新展开保留此前的展示数量。
- 搜索时展示匹配任务，清空搜索恢复原分类折叠状态。

## 发布验证

- Android JVM：52/52；Android 16 模拟器界面回归：21/21。
- `lintPreview`、签名 Preview 构建通过；lint 无错误，保留 18 项警告。
- `npm test`：307/307。
- 复用持久签名，通过包名、SDK、ARM64、安装标记、ZIP 和 16KB 对齐检查。
- 模拟器以 `adb install -r` 从 0.4.0 覆盖升级，未卸载、未使用 `-t`；安装时间和应用 UID 保持不变，新版启动正常。
- 固定下载页保留全部历史版本；公网 APK 重新下载后的 SHA-256、签名及 HTTP 范围下载校验通过。
- 390px 深色、1280px 浅色布局及下载链接检查通过。公网 HTTPS 返回的 HTML 与本地 Caddy 页面完全一致；本环境 Chromium 公网导航返回 `ERR_EMPTY_RESPONSE`，视觉验证使用该本地页面，未将 HTTP 获取成功表述为公网浏览器验收通过。

文件：`codex-native-android-0.4.1-preview.apk`，13,167,806 字节。

SHA-256：`0a2486a1ce02e4c370773e5496b2893c0143c28d002620f2c827fd74c239f800`。

仍为预览版，未完成真机、魅族/Flyme 和真实账户的完整验收；模拟器结果不代表所有设备。已有同签名预览版请直接覆盖安装，不要先卸载。
