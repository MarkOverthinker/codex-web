# 0.4.2-preview 本地候选包

构建日期：2026-09-12。版本代码：11。包名：`app.codexweb.mobile.preview`。

**状态：本地已构建和验证，尚未发布到公开下载页。** 下载页仍为 0.4.1；需要确认后按 `docs/ANDROID_RELEASES.md` 发布并验收，不将本地 APK 或模拟器安装视为已发布。

## 更新

- 底栏只保留对话、工作台、我的三个 24dp 图标，移除文字、悬浮底座、边框、阴影、选中底色和短标记。
- 导航内容高度由 80dp 收紧至 48dp（均不含系统底部安全区），三个入口等宽排列；选中项用实心图标与前景色区分，点击反馈限制为小圆形。
- 保留完整触控区域、无障碍名称、Tab 选中语义和系统安全区；键盘打开或进入工具子页时继续隐藏导航。
- 切换入口继续保留草稿与聊天阅读位置，不改动输入框、后端 API、存储或账号隔离。

## 已完成验证

- Android JVM：52/52；最终源码的 Android 16 模拟器界面回归：22/22。
- 额外通过 360dp 浅色、411dp 深色及 1.5 倍系统字体的导航验证和截图检查：均为 48dp 高、三个图标、无可见文字，选中与无障碍语义正确。
- 新增键盘展开隐藏底栏、收起恢复及保留草稿测试；保留工具子页隐藏、切换时草稿与阅读位置的回归。
- `lintPreview`、Debug/测试包和签名 Preview 构建通过；lint 无错误，保留 18 项既有警告。
- `npm test`：307/307。
- 复用持久签名，通过包名、SDK、ARM64、非调试/非测试标记、ZIP 和 16KB 对齐检查。
- 模拟器以 `adb install -r` 从 0.4.1 覆盖升级，未卸载、未使用 `-t`；应用 UID 和首次安装时间保持不变，0.4.2 启动成功。

本地 APK：`android/app/build/outputs/apk/preview/app-preview.apk`，13,167,806 字节。

SHA-256：`069956ae60792244535b5df2333f8fce3be0c87209b00071cacec45c252dde8c`。

## 待完成

- 获得公开发布确认后，使用维护的发布脚本更新固定下载页，保留历史 APK、校验文件与版本记录。
- 验证新版本公开页面、HTTPS APK 重新下载及 SHA-256、签名和 HTTP 范围下载。上述新版本公网检查尚未执行。
- 真机、魅族/Flyme 与真实账户完整验收仍未完成；模拟器测试使用模拟 API，不代表全部设备或生产账户场景。
